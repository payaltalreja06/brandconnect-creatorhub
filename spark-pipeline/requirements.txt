

# ── Core ETL (etl.py) ──────────────────────────────────────────────────────────
pyspark==3.5.0

# ── Local dev convenience ──────────────────────────────────────────────────────
# Lets you store YT_API_KEY in a .env file instead of setting env vars manually
# Usage in fetch_channel.py or any script: from dotenv import load_dotenv; load_dotenv()
python-dotenv==1.0.1

pymongo[srv]==4.7.2
certifi==2024.2.2
dnspython==2.6.1

# mongo-spark-connector        # Spark-native MongoDB connector (via spark-submit --packages)

# ── Future: Instagram / Facebook ETL (uncomment when modules are added) ────────
# requests==2.31.0             # HTTP client for Meta Graph API calls
